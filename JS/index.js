console.log("Decode!");

const helper = (data, k, storage) => {
	if (k === 0) return 1;
}

const numberOfWays = (data) => {
	// To store result
	let storage = {};
	helper(data, data.length, storage);
}